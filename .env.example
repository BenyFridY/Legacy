# Copie para .env e preencha. O .env real é gitignored — NÃO commitar.

# Stack de geração — ver ADR-0003. Hoje só `groq_free` está plugado (requer GROQ_API_KEY abaixo).
# Deixe COMENTADO para rodar SEM LLM: o sistema ainda roteia, recupera, computa e CITA — só não
# redige texto livre (fallback honesto). gemini_free/ollama/anthropic/claude_code = interface
# prevista, não implementada (ver legacy_rag/generation/__init__.py).
# LLM_PROVIDER=groq_free

# Chaves opcionais (só se usar o provider correspondente):
# GEMINI_API_KEY=
# GROQ_API_KEY=
# ANTHROPIC_API_KEY=

# Identificação para a API do SEC EDGAR — NÃO usada no Caso B (fontes: mziq/CDN + Bacen); fica pronta:
SEC_USER_AGENT=Legacy Case - beny.frid@hashdex.com

# Store DuckDB (gitignored):
DUCKDB_PATH=data/legacy.duckdb
