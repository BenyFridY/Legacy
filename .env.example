# Copie para .env e preencha. O .env real é gitignored — NÃO commitar.

# Stack de geração — ver ADR-0003. Default open/free.
# Opções: claude_code | gemini_free | groq_free | ollama | anthropic
LLM_PROVIDER=claude_code

# Chaves opcionais (só se usar o provider correspondente):
# GEMINI_API_KEY=
# GROQ_API_KEY=
# ANTHROPIC_API_KEY=

# Identificação obrigatória na API do SEC EDGAR (sem User-Agent = HTTP 403):
SEC_USER_AGENT=Legacy Case - beny.frid@hashdex.com

# Store DuckDB (gitignored):
DUCKDB_PATH=data/legacy.duckdb
